# Hermes Shield false block and routing verification — 2026-09-10

## DEBUG REPORT

- **Symptom:** A benign Hermes request returned HTTP 400 with Shield score 67 and detections for jailbreak, `CLAUDE.md`, suspicious HTML comments, and `sudo`; Hermes verification continued to report zero traffic. Review connection changes appeared to do nothing.
- **Root cause:** The LiteLLM callback flattened every message role into the inbound threat scan. Hermes sends its trusted generated system prompt as a `system` message; that 32,983-character prompt contains legitimate security, context-file, comment-marker, and terminal instructions. The operator's two-character and 25-character user messages independently scored zero. Review was disabled because the selected Hermes route was already applied and had zero pending changes.
- **Fix:** Inbound scanning now excludes trusted `system`, `developer`, and previously scanned `assistant` content while continuing to scan `user`, `tool`, and unknown roles. The behavior is in the shared LiteLLM callback, so Hermes, OpenClaw, and OpenCode use the same rule.
- **Evidence:** After restarting LiteLLM as PID `62424`, a real Hermes `hello` request completed with HTTP 200. ClawNex recorded a verified outbound event at `2026-09-09T22:06:35.827Z` for `openrouter/deepseek/deepseek-v4-flash-0731`, connector `hermes`, source `hermes:home:d3403a46447d`, with proxy request ID and 16,293 tokens. Live verification returned `protected-and-verified` with one matching event and reported cost.
- **Regression test:** `scripts/verify-routing-logger-evidence.py` exercises the callback's public pre-call hook with noisy trusted context plus benign user/tool content. `scripts/verify-native-routing-proxy.py` proves signed attribution for Hermes, OpenClaw, and OpenCode through a disposable real LiteLLM proxy, including streaming and fail-closed paths.
- **Related:** The callback-instance registration defect fixed in `c9550e9` was global. OpenCode's post-apply alias correction was connector-specific. The current OpenClaw configuration is direct, so live routed OpenClaw acceptance requires a future reviewed apply; the disposable native test covers its shared callback/identity path without a paid call.
- **Status:** DONE — Hermes false blocking and zero-evidence verification are fixed and proven with real traffic. The Review button behavior is correct for an already-applied route, though a clearer disabled-state label may be considered separately.
